// Do not reference this file directly. Reference environment.ts and the build will replace it with this.

import { Environment } from "./environment.interface";

export const environment: Environment = {
  production: true,
};
